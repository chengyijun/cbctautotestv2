from os.path import abspath, dirname, join

# 项目路径
ROOT_PATH = abspath(dirname(__file__))
# 图片检查点路径
CP_IMAGES_PATH = join(ROOT_PATH, 'cp_images')
# 配置文件路径
BASE_CONFIG_PATH = join(ROOT_PATH, 'config')
